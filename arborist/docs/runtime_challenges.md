# Agents should record all issues they run into here
Feb 3, 2026: `scripts/pre_commit.sh` failed at the Biome step because `npx` could not reach the npm registry (EAI_AGAIN). It also reported it could not write npm logs to `/home/hogan/.npm/_logs`.
Feb 3, 2026: `scripts/setup.sh` failed because `npm install` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed again because `npx` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed after the template move because `npx` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed while adding discovery tests because `npx` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed again during final checks because `npx` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed while updating list cleanup because `npx` could not reach the npm registry (EAI_AGAIN).
Feb 3, 2026: `scripts/pre_commit.sh` failed during documentation updates because `npm` could not reach the npm registry (EAI_AGAIN) for Biome.
Feb 3, 2026: `scripts/pre_commit.sh` failed again at the Biome step due to npm registry connectivity (EAI_AGAIN) and reported it could not write logs to `/home/hogan/.npm/_logs`.
Feb 3, 2026: `scripts/pre_commit.sh` failed at the Biome step due to npm registry connectivity (EAI_AGAIN) and reported it could not write logs to `/home/hogan/.npm/_logs`.
Feb 3, 2026: `scripts/pre_commit.sh` failed at the Biome step because `npm` could not reach the npm registry (EAI_AGAIN) and reported it could not write logs to `/home/hogan/.npm/_logs`.
Feb 3, 2026: `scripts/pre_commit.sh` failed again at the Biome step because `npm` could not reach the npm registry (EAI_AGAIN) and reported it could not write logs to `/home/hogan/.npm/_logs`.
Feb 3, 2026: `scripts/pre_commit.sh` failed at the Biome step because `biome.json` used the deprecated `files.ignore` key that is no longer recognized by Biome 2.3.13.
Feb 3, 2026: `scripts/pre_commit.sh` failed running `pre-commit` because it could not write to the default cache database or log paths under `/home/hogan/.cache` and `/home/hogan/.cache/pre-commit/pre-commit.log`.
Feb 3, 2026: `scripts/pre_commit.sh` failed running `pre-commit` because git could not fetch `https://github.com/pre-commit/pre-commit-hooks/` (Could not resolve host: github.com).
